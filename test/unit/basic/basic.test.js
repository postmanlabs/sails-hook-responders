/**
 * Basic tests to check whether sails has successfully loaded with the responders mapped to the
 * correct controller actions
 */

describe('Basic hook test', function () {
  it('sails starts successfully', function () {
    return true;
  });
});
